<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.modern5.voicebasedemailv2">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".Adisplay_msg"></activity>
        <activity android:name=".AEmails" />
        <activity android:name=".display_msg" />
        <activity android:name=".Emails" />
        <activity android:name=".Asend" />
        <activity android:name=".Asendofrecieve" />
        <activity android:name=".loginArabic" />
        <activity android:name=".activity_send" />
        <activity android:name=".sendofrecieve" />
        <activity android:name=".activity_login" />
        <activity android:name=".language" />


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>